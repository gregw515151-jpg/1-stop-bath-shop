services:
  - type: web
    name: one-stop-bath-shop
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: RESEND_API_KEY
        sync: false
